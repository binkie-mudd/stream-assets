document.addEventListener('onLoad', function(obj) {
  // widget loaded
});

document.addEventListener('onEventReceived', function(obj) {
  // Streamlabs handles chat rendering via the template above
  // This is here for any extra custom logic if needed
});